# Attack Path Story Demo Agent

This inert Homi-like workspace is synthetic demo input for AgentSec. It is
never executed. The override and MCP declarations intentionally create a
static, reviewable path that can be associated with deterministic and semantic
Evidence.
