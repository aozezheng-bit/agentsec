# Demo Acceptance

- All files are inert text fixtures.
- No command, Skill, Hook, MCP server, or Provider is executed.
- All external locations use `example.invalid`.
- No credential or secret value is included.
- AgentSec remains report-only; static paths do not prove runtime reachability.
